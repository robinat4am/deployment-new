name: Release Please

on:
  push:
    branches: [develop]

jobs:
  release-please:
    runs-on: windows-latest
    steps:
      - name: Release Please
        uses: googleapis/release-please-action@v4
        with:
          release-type: node
          package-name: deployment
